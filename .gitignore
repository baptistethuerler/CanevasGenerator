# Dépendances (locales à chaque machine, jamais synchronisées)
node_modules/
node_modules.nosync/

# Build
dist/
build/
app/dist/

# Compagnon visuel de brainstorming (maquettes temporaires)
.superpowers/

# Exports générés
exports/

# Créations & uploads (data/, images/, logos/, fonts/) : SYNCHRONISÉS via GitHub.
# (Les exports vidéo/PNG générés, eux, restent ignorés — voir « Exports générés » ci-dessus.)

# Dossiers temporaires de test
.tmp-test-*

# macOS
.DS_Store

# Dépôt local de fonds (importés dans images/ au démarrage)
fonds/
